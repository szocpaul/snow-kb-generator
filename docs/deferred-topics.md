# Deferred topics

Digressions cut from Getting Started, candidates for Diving Deeper.

- Why the template is mandatory and the legacy draft/format path was removed (spec 009) — cut from "Following the pipeline" for flow.
- pi-auth (`use_pi_auth: true`) as an alternative to OPENAI_API_KEY — alternative-path, belongs in the config DD topic.
- The full guardrail list and the three lines of hallucination defense (spec 004/007) — too much detail mid-tutorial.
- Team-based template selection by assignment group (spec 002) — tutorial uses the Integration Team template only.
- SkilledProposer and anti-overfitting instruction proposals (spec 007) — GEPA internals, DD.
- `force_update` semantics beyond the 409 mention — DD templates/duplicates topic.
- Docker/docker-compose deployment — DD webhook topic; tutorial runs uvicorn directly.
